#ifndef MAIN_H
#define MAIN_H

#include <stdarg.h>
#include <string.h>
#include <unistd.h>

int _partwel(char c);
int print_string(const char *str);
int print_char(const char c);
int _printf(const char *format, ...);

#endif
